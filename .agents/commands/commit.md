---
description: "Safe commit with Conventional Commits."
argument-hint: "[scope/message hint]"
---
Stage only explicit paths (never `git add .`). Review !`git diff --staged`. Write a Conventional Commit (feat|fix|refactor|build|ci|chore|docs|style|perf|test). Do not push unless asked. Hint: $ARGUMENTS
